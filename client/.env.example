# Client Environment Variables Template (Optional)
# By default, the client uses relative '/api' which works seamlessly with Vite dev proxy and Nginx on VPS.
# If your backend API is hosted on a separate external domain, uncomment and set the URL below:

# VITE_API_URL=https://your-api-domain.com/api
